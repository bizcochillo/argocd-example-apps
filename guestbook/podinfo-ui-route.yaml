apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: frontend-demo
  namespace: guestbook
spec:
  to:
    kind: Service
    name: frontend-demo
    weight: 100
  port:
    targetPort: http
  tls:
    termination: edge
